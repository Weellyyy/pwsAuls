<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Konser - Info Konser</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="navbar">
            <h1>🎵 Info Konser - Admin</h1>
            <a href="/admin/dashboard">← Kembali</a>
        </div>

        <div class="content">
            <div class="header">
                <h2>Edit Konser</h2>
            </div>

            <% if (error && error.length > 0) { %>
                <div class="alert alert-error"><%= error %></div>
            <% } %>

            <form method="POST" action="/admin/concerts/edit/<%= concert.id %>" enctype="multipart/form-data">
                <div class="form-group">
                    <label>Judul Konser *</label>
                    <input type="text" name="title" required value="<%= concert.title %>">
                </div>

                <div class="form-group">
                    <label>Artist/Penyanyi *</label>
                    <input type="text" name="artist" required value="<%= concert.artist %>">
                </div>

                <div class="form-group">
                    <label>Tanggal *</label>
                    <input type="date" name="date" required value="<%= new Date(concert.date).toISOString().split('T')[0] %>">
                </div>

                <div class="form-group">
                    <label>Waktu *</label>
                    <input type="time" name="time" required value="<%= concert.time %>">
                </div>

                <div class="form-group">
                    <label>Lokasi *</label>
                    <input type="text" name="location" required value="<%= concert.location %>">
                </div>

                <div class="form-group">
                    <label>Harga Tiket (Rp) *</label>
                    <input type="number" name="price" required value="<%= concert.price %>">
                </div>

                <div class="form-group">
                    <label>Deskripsi</label>
                    <textarea name="description"><%= concert.description || '' %></textarea>
                </div>

                <div class="form-group">
                    <label>Gambar Konser</label>
                    <% if (concert.image_url) { %>
                        <div style="margin-bottom: 10px;">
                            <img src="<%= concert.image_url %>" alt="Current Image" style="max-width: 300px; max-height: 200px; object-fit: cover; border-radius: 5px;">
                            <p style="font-size: 12px; color: #666; margin-top: 5px;">Gambar saat ini</p>
                        </div>
                    <% } %>
                    <input type="file" name="image" accept="image/*">
                    <small>Upload gambar baru untuk mengganti (Format: JPG, PNG, GIF, Maks 5MB). Kosongkan jika tidak ingin mengganti.</small>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Update</button>
                    <a href="/admin/dashboard" class="btn btn-secondary">Batal</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
